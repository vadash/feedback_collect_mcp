{
  "name": "feedback-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for collecting user feedback via WPF GUI",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.11.3",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2"
  }
}
